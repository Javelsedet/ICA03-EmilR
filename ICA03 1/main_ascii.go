package main

import "./ascii"

func main() {

	ascii.GreetingASCII()

	/*
	   Skriver ut i på hex, des, binær og char. A, B, C og D
	   er ny linje, tab og lignende, så det ser feil, men er riktig.
	*/
	//for i := 1; i < 129; i++ {
	//fmt.Printf("Hex: %X Symbol: %c Binær: %b Desimal: %d\n", i, i, i, i)
}

//go ascii.IterateOverASCIIStringLiteral(ascii, Ascii)
/*
	    Oppg 1a)
	       a := os.Args[1:]
		   	fmt.Printf("%b\n", a) //%b viser variabel a med base 2.
		   	for i := 0; i < len(a); i++ {
		   		fmt.Printf("%b\n", a[i])
		   	}

*/
